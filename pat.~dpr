program pat;

uses
  Forms,
  login in 'login.pas' {Form1},
  Register in 'Register.pas' {Form2},
  Home in 'Home.pas' {Form3},
  Donations in 'Donations.pas' {Form4},
  EventsView in 'EventsView.pas' {Form5},
  GalleryView in 'GalleryView.pas' {Form6},
  PhotoView in 'PhotoView.pas' {Form7},
  News in 'News.pas' {Form8},
  ContactUs in 'ContactUs.pas' {Form9},
  AboutUs in 'AboutUs.pas' {Form10},
  forumHome in 'ForumHome.pas' {Form11},
  categoryView in 'CategoryView.pas' {Form12},
  topicView in 'TopicView.pas' {Form13},
  NewsAuthentication in 'NewsAuthentication.pas' {Form14},
  NewArticle in 'NewArticle.pas' {Form15};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm3, Form3);
  Application.CreateForm(TForm4, Form4);
  Application.CreateForm(TForm5, Form5);
  Application.CreateForm(TForm6, Form6);
  Application.CreateForm(TForm7, Form7);
  Application.CreateForm(TForm8, Form8);
  Application.CreateForm(TForm9, Form9);
  Application.CreateForm(TForm10, Form10);
  Application.CreateForm(TForm11, Form11);
  Application.CreateForm(TForm12, Form12);
  Application.CreateForm(TForm13, Form13);
  Application.CreateForm(TForm14, Form14);
  Application.CreateForm(TForm15, Form15);
  Application.Run;
end.
